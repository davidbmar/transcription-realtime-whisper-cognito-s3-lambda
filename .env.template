# ============================================================================
# transcription-realtime-whisper-cognito-s3-lambda-ver4
# Environment Configuration Template
# Generated by: scripts/source/000-questions.sh
# ============================================================================

# ============================================================================
# AWS Account Configuration
# ============================================================================
AWS_REGION={{AWS_REGION}}
AWS_ACCOUNT_ID={{AWS_ACCOUNT_ID}}

# ============================================================================
# GPU Infrastructure (WhisperLive)
# ============================================================================
GPU_INSTANCE_ID={{GPU_INSTANCE_ID}}               # Discovered after deployment
GPU_INSTANCE_IP={{GPU_INSTANCE_IP}}               # Discovered at startup
GPU_INSTANCE_TYPE={{GPU_INSTANCE_TYPE}}           # e.g., g4dn.xlarge
GPU_SPOT_ENABLED={{GPU_SPOT_ENABLED}}             # true/false
SSH_KEY_NAME={{SSH_KEY_NAME}}
SECURITY_GROUP_ID={{SECURITY_GROUP_ID}}           # Discovered after creation
BUILDBOX_SECURITY_GROUP={{BUILDBOX_SECURITY_GROUP}} # Discovered
BUILDBOX_PUBLIC_IP={{BUILDBOX_PUBLIC_IP}}         # Discovered

# ============================================================================
# Storage (S3)
# ============================================================================
# WhisperLive models bucket
S3_MODEL_BUCKET={{S3_MODEL_BUCKET}}               # e.g., dbm-cf-2-web

# User data and transcripts
S3_BUCKET_NAME={{S3_BUCKET_NAME}}                 # Created by scripts
CLOUDFRONT_DISTRIBUTION_ID={{CLOUDFRONT_DISTRIBUTION_ID}}  # Discovered
CLOUDFRONT_URL={{CLOUDFRONT_URL}}                 # Discovered

# ============================================================================
# Authentication (Cognito)
# ============================================================================
COGNITO_USER_POOL_ID={{COGNITO_USER_POOL_ID}}     # Discovered after creation
COGNITO_CLIENT_ID={{COGNITO_CLIENT_ID}}           # Discovered
COGNITO_IDENTITY_POOL_ID={{COGNITO_IDENTITY_POOL_ID}}  # Discovered
COGNITO_DOMAIN={{COGNITO_DOMAIN}}                 # Discovered

# ============================================================================
# API Gateway
# ============================================================================
API_GATEWAY_URL={{API_GATEWAY_URL}}               # Discovered after deploy
AUDIO_API_ENDPOINT={{AUDIO_API_ENDPOINT}}         # Discovered

# ============================================================================
# Billing (Stripe)
# ============================================================================
STRIPE_SECRET_KEY={{STRIPE_SECRET_KEY}}           # From user input
STRIPE_PUBLISHABLE_KEY={{STRIPE_PUBLISHABLE_KEY}} # From user input
STRIPE_WEBHOOK_SECRET={{STRIPE_WEBHOOK_SECRET}}   # Generated

# ============================================================================
# Application Settings
# ============================================================================
APP_DOMAIN={{APP_DOMAIN}}                         # e.g., api.yourdomain.com
APP_NAME={{APP_NAME}}                             # e.g., transcription-api
APP_HOST={{APP_HOST}}                             # Default: 0.0.0.0
APP_PORT={{APP_PORT}}                             # Default: 8443

# ============================================================================
# Deployment Metadata
# ============================================================================
DEPLOYMENT_ID={{DEPLOYMENT_ID}}                   # Generated: $(date +%Y%m%d-%H%M%S)
DEPLOYMENT_TIMESTAMP={{DEPLOYMENT_TIMESTAMP}}     # Generated
CONFIG_VERSION={{CONFIG_VERSION}}                 # Template version
