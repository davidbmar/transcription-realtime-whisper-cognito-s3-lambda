# Requirements for speaker diarization (run on GPU instance)
# Install with: pip install -r requirements-diarization.txt

# PyAnnote audio - speaker diarization
# Requires:
#   1. Accept license at: https://huggingface.co/pyannote/speaker-diarization-3.1
#   2. Set HF_TOKEN environment variable with your token
pyannote-audio>=3.3.0

# PyTorch - required by pyannote (should already be installed on GPU instance)
# torch>=2.0.0

# Additional dependencies (usually installed with pyannote-audio)
# torchaudio
# transformers
# asteroid-filterbanks
# speechbrain

# Note: Models should be pre-cached in ~/.cache/huggingface/
# Download from: s3://dbm-cf-2-web/bintarball/diarized/latest/huggingface-cache.tar.gz
